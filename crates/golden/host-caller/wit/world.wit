package sammyne:host-caller@1.0.0;

interface api {
    multi-sleep: func(times: u64, duration-ms: u64);
}

/// An example world for the component to target.
world host-caller-world {
    export api;

    import sammyne:host/api@1.0.0;
}
